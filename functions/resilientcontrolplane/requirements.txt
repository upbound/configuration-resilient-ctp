crossplane-function-sdk-python==0.9.0
pydantic==2.12.4
# Cloud SDKs for spec.heartbeat.read=directApi (cloud_read.py). Imported lazily,
# but MUST be present in the function image or the direct peer-read raises
# CloudReadError -> readable=False -> the election fail-safe holds the secondary
# at standby forever (silently). All three ship so directApi works for any
# provider, including cross-cloud sets where one CP reads a peer on another cloud.
boto3==1.34.162
azure-identity==1.19.0
azure-mgmt-resource==23.2.0
google-cloud-storage==2.19.0
